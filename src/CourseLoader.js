// function loadCourses(courseList) {
export function CourseLoader() {
    console.log("testing");
    // const courseMap = new Map();
    // const lines = courseList.trim().split('\n');
    // lines.shift();

    // for (const line of lines) {
    //     const lineData = line.split(",");

    //     const courseString = lineData[0];
    //     const sectionString = lineData[1];
    //     const titleString = lineData[2];
    //     const dateString = lineData[4];
    //     const timeString = lineDate[5];
    //     const instructorString = lineData[6];

    //     if (!courseMap.has(courseString)) {
    //         courseMap.set(courseString, new Course(courseString));
    //     }
    //     const course = courseMap.get(courseString);

    //     const ID = sectionString.trim().split(' ')[0];
    //     const CRN = sectionString.split('(')[1].split(')')[0];

    //     const title = titleString.subString(0, titleString.lastIndexOf('(')).trim();
    //     const credits = titleString.subString(titleString.lastIndexOf('(')+1, titleString.lastIndexOf(')'))

    //     const startDate = dateHelper();
    // }
}

// function dateHelper(string) {
//     const year = 2025
//     var month;
//     var day;



//     if (!year || !month || !day) return;

//     return new Date(year, month, day);
// }
